/**
 * @title User Authentication API Documentation
 * @description Comprehensive documentation for user and admin authentication APIs.
 */

/**
 * Logs in a user with the provided credentials.
 * 
 * @function loginUser
 * @description Authenticates a user and returns their data upon successful login.
 * @endpoint POST  api/user/login
 * @param {string} email - The email address of the user.
 * @param {string} password - The password of the user.
 * @returns {Promise<Object>} A promise that resolves to the user data or an error object.
 */

/**
 * Registers a new user with the provided details.
 * 
 * @function registerUser
 * @description Creates a new user profile in the system and registers the user.
 * @endpoint POST api/user/register
 * @param {string} email - The email address of the new user.
 * @param {string} password - The password for the new user.
 * @param {Object} [additionalData] - Optional additional data for the user profile (e.g., name, phone number).
 * @returns {Promise<Object>} A promise that resolves to the registered user data or an error object.
 */

/**
 * Logs in an admin user with the provided credentials.
 * 
 * @function adminLogin
 * @description Authenticates an admin user and returns their data upon successful login.
 * @endpoint POST api/user/admin
 * @param {string} adminEmail - The email address of the admin.
 * @param {string} adminPassword - The password of the admin.
 * @returns {Promise<Object>} A promise that resolves to the admin user data or an error object.
 */

/**
 * Firebase authentication instance for managing user authentication.
 * @endpoint POST api/user/google
 * @constant {Object} firebaseAuth
 * @description Provides methods for user authentication, including login, logout, and registration.
 */